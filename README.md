# Fictional Profile Generator

Vercel-ready Next.js app for generating fictional/test profile records.

## Run locally
```bash
npm install
npm run dev
```

## Deploy
Import this folder into Vercel, or push it to a Git repository and import the repository in Vercel.

This app does NOT create Facebook accounts or connect to Facebook. Phone values are explicit test placeholders.
